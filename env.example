# MongoDB — Get a free cluster from https://www.mongodb.com/atlas
# Create a database, get the connection string, and paste it here.
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hwb2026?retryWrites=true&w=majority
BASE_URL=https://hwb-attendee-profile.vercel.app
EMAIL_FROM=Heritage Without Borders <hwbsanfo101@gmail.com>

# Brevo SMTP — Settings → SMTP & API in your Brevo dashboard
# SMTP Login: found under "Your SMTP Settings" (e.g. ab201e001@smtp-brevo.com)
# API Key: generated under Settings → API Keys (also used as the SMTP password)
BREVO_SMTP_LOGIN=your-smtp-login@smtp-brevo.com
BREVO_API_KEY=your-brevo-api-key

# Admin password — used to protect the /admin page
ADMIN_PASSWORD=your-secret-password-here
